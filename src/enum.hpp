
enum ESample
{
    Eat,
    StarS,
    Death,
    Music1
};

enum SoundType
{
    Music,
    Effect
};

enum BodyType
{
    RBODY,
    GBODY,
    BBODY,
    NBODY
};

enum Block
{
    R,
    G,
    B,
    Empty,
    Star
};

enum Direction
{
    HAUT,
    BAS,
    GAUCHE,
    DROITE
};

enum Statut
{
    Play,
    Pause,
    Begin,
    GameOver,
    Load,
    Save
};
