
enum ESample
{
    Break,
    Bong,
    Music1,
    Music2
};

enum SoundType
{
    Music,
    Effect
};

enum BodyType
{
    RBODY,
    GBODY,
    BBODY,
    NBODY
};

enum Block
{
    R,
    G,
    B,
    Empty,
    Star
};

enum Direction
{
    HAUT,
    BAS,
    GAUCHE,
    DROITE
};

enum Statut
{
    Play,
    Pause,
    Begin,
    GameOver,
    Load
};
